class Player {
    constructor(id, playerSkin, hairColor, headSize) {
        this.id = id;
        this.playerSkin = playerSkin;
        this.hairColor = hairColor;
        this.headSize = headSize;
        this.hand = []; // empty hand to start with
    }
}